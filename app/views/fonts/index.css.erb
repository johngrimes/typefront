<% @fonts.each do |font| %>
@font-face {
  font-family: "<%= font.name %>";
  src: url(<%= font_path(:id => font.id, :format => 'font') %>);
}

.font-<%= font.id %> {
  font-family: "<%= font.name %>", Helvetica Neue, Helvetica, Arial, sans-serif;
}

<% end %>
