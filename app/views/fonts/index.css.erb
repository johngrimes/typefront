<% @fonts.each do |font| %>
<%= include_code(font, :unique_font_names => true) %>

.font-<%= font.id %> {
  font-family: "<%= font.font_family %> <%= font.id %>", Helvetica, Arial, sans-serif;
<%= style_descriptors(font) %>
}

<% end %>
