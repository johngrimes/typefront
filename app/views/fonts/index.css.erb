<% @fonts.each do |font| %>
@font-face {
  font-family: "<%= font.font_family %>";
  src: url(<%= font_path(:id => font.id, :format => 'otf') %>);
}

.font-<%= font.id %> {
  font-family: "<%= font.font_family %>", Helvetica Neue, Helvetica, Arial, sans-serif;
}

<% end %>
